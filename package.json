{
  "name": "at-finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "local": "vite dev",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "latest",
    "@types/node": "^22.18.6",
    "typescript": "^5.9.3",
    "vite": "latest",
    "wrangler": "latest"
  },
  "dependencies": {
    "@fettstorch/jule": "^3.1.0"
  }
}
